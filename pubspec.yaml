name: ilia_challenge
description: A flutter challenge project, to job a flutter dev position at ilia.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  dio: 4.0.1
  rxdart: 0.27.2
  devicelocale: 0.5.0
  dio_http_cache: 0.3.0
  cached_network_image: 3.1.0


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true